---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-dagobah-applications
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
  - name: infra-dagobah-configs
  path: ./apps/dagobah/applications
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-dagobah-configs
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
  - name: apps-dagobah-applications
  path: ./apps/dagobah/configs
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg