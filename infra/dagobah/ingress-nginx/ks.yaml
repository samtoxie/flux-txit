---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingres-nginx-base
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infra/dagobah/ingres-nginx
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingres-nginx-conf
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: ingres-nginx-base
  path: ./infra/dagobah/ingres-nginx/conf
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
